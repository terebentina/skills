interface:
  display_name: "To Tickets"
  short_description: "Split a plan into tracer-bullet tickets"
  default_prompt: "Use $to-tickets to split this approved spec into tickets."
policy:
  allow_implicit_invocation: false
