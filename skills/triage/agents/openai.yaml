interface:
  display_name: "Triage"
  short_description: "Move issues through triage roles"
  default_prompt: "Use $triage to process the current issue queue."
policy:
  allow_implicit_invocation: false
