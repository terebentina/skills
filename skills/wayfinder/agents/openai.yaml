interface:
  display_name: "Wayfinder"
  short_description: "Map a large effort as decision tickets"
  default_prompt: "Use $wayfinder to map this large effort as decision tickets."
policy:
  allow_implicit_invocation: false
