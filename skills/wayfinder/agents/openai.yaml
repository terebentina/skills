interface:
  display_name: "Wayfinder"
  short_description: "Explore an effort in local decision files"
  default_prompt: "Use $wayfinder to explore this effort under .scratch/wayfinder only."
policy:
  allow_implicit_invocation: false
