interface:
  display_name: "Handoff"
  short_description: "Prepare a compact handoff for another agent"
  default_prompt: "Use $handoff to prepare this work for the next session."
policy:
  allow_implicit_invocation: false
