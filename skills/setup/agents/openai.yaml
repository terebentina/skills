interface:
  display_name: "Setup"
  short_description: "Configure a repo for the engineering skills"
  default_prompt: "Use $setup to configure this repository for the engineering skills."
policy:
  allow_implicit_invocation: false
