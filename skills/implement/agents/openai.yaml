interface:
  display_name: "Implement"
  short_description: "Build work from a spec or tickets"
  default_prompt: "Use $implement with ticket ABC-123 to complete the planned work."
policy:
  allow_implicit_invocation: false
