interface:
  display_name: "To Spec"
  short_description: "Turn a conversation into a spec"
  default_prompt: "Use $to-spec to publish this conversation as a spec."
policy:
  allow_implicit_invocation: false
